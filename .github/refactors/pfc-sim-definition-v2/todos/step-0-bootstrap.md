# Step 0 — Bootstrap the Refactor

Purpose: Create the refactor workspace for `pfc-sim-definition-v2` and add initial plan/todos.

Status: completed

Checklist:
- [x] Create folder `.github/refactors/pfc-sim-definition-v2/`.
- [x] Create `plan.md` (copied from source) under the refactor folder.
- [x] Create `todos/` directory for step files.
- [x] Add this `step-0-bootstrap.md` file.

Exit criteria:
- The refactor folder contains `plan.md` and an empty `todos/` (now populated).

Notes (constraints):
- Keep this step strictly as setup; do not modify production code here.
- Follow the repository PFC refactor conventions for file paths and naming.

Progress update:
- Bootstrapped refactor workspace. Ready for analysis and extraction.